func nestedWork(n: int): int {
    let total = 0;
    let i = 0;
    while (i < n) {
        let j = 0;
        while (j < n) {
            total = total + i * j;
            j = j + 1;
        }
        i = i + 1;
    }
    return total;
}

print(nestedWork(200));
