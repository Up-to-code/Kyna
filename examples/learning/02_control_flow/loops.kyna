var sum = 0;
var index = 1;
while (index <= 4) {
    sum = sum + index;
    index = index + 1;
}

var product = 1;
loop (var value = 1; value <= 4; value = value + 1) {
    product = product * value;
}

print("loops", sum, product);
