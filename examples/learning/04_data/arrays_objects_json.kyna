var numbers = [3, 1, 2];
numbers[0] = 4;
push(numbers, 5);

const project = { name: "Kyna", version: 1, tags: ["language", "backend"] };
const encoded = json.stringify(project);
const decoded = json.parse(encoded);

print("array", json.stringify(numbers), pop(numbers), len(numbers));
print("object", decoded.name, decoded.tags[1], keys(decoded));
