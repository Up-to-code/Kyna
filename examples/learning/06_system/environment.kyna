# The host injects environment access. Missing variables produce null.
const value: str? = process.env("KYNA_LEARNING_VALUE");
const missing: str? = process.env("KYNA_LEARNING_MISSING_91F4");
print("environment", value, missing == null);
