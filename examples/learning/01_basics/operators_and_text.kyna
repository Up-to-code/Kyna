const total = 20 + 22;
const comparison = total == 42 && total > 10;
const greeting = textReplace("Hello student", "student", "Kyna");
const unicode = "Héllo 世界";

print("operators", total, comparison, 17 % 5);
print("text", greeting, len(unicode), textSlice(unicode, 6));
