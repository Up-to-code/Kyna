# const cannot be reassigned. var is mutable but remains type-checked.
const fixed = 10;
var changing: int = 10;
changing = changing + 5;

# Uncommenting `fixed = 20;` produces an immutable-binding diagnostic.
# Uncommenting `changing = "wrong";` produces a type diagnostic.
print("bindings", fixed, changing);
