# Public no-key HTTP helper smoke test. Network availability is not required by CI.
try {
    const payload = httpGet("https://jsonplaceholder.typicode.com/todos/1");
    const todo = jsonParse(payload);
    print("todo", todo.id, todo.completed);
} catch (failure) {
    print("HTTP smoke test unavailable", failure.code, failure.message);
}
