# Process, environment, and clock APIs. Commands stay disabled for portability.
const executablePath = processEnv("PATH");
sleep(0);
wait(0);

if (false) {
    processRun("echo Kyna");
    build("echo Kyna");
}

print("process", typeOf(executablePath));
