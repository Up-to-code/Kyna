[project]
name = "backend"
version = "0.1.0"
entry = "src/main.kyna"
template = "backend"

[server]
host = "127.0.0.1"
port = 3000

[scripts]
check = "ky check"
test = "ky check tests"

[dependencies]
